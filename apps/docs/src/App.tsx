// Docs / component-playground app.
//
// This will grow into the live docs site for @sketch-ui/react: import
// components from '@sketch-ui/react' here and demo them page by page (or
// wire up react-router if you want real routes).
export function App() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: 40 }}>
      <h1>sketch-ui docs</h1>
      <p>Add component demos here as you build them in packages/ui.</p>
    </main>
  );
}
